# Apple Review Notes

- Demo account email: `apple-review@kurbada.app`
- Access model: this account is seeded with `profiles.access_override='apple_review'` so the app remains fully reviewable while RevenueCat stays disabled in development.
- Backend requirement: keep the Supabase project live and reachable during review.
- RevenueCat status: implemented in-app, but `EXPO_PUBLIC_REVENUECAT_ENABLED=false` for development and review builds until subscriptions are ready to turn on.
